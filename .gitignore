# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
game/
game

# 🗜️ 素材：只准 WebP 进仓库。
# 原始 PNG/JPG 是 AI 出图的全分辨率输出（立绘 ~3MB、背景 ~10MB），
# 转成 WebP 后小 27 倍且肉眼无差别。新素材丢进 public/images 后跑：
#   npm run assets
# 转换完原图会自动移到 .backup-originals/（本地保留，不进 git）。
public/images/**/*.png
public/images/**/*.jpg
public/images/**/*.jpeg
.backup-originals/
_git-backup-*.bundle

# AI 生成立绘的暂存区：审过之后用 --adopt 搬进 public 才入库
.generated/

# Environment variables (Never commit real API keys to git)
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local
!.env.example

# Claude Code local settings (machine-local, never commit — may contain paths/tokens)
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
